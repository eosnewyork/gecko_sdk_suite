/** @brief
 *
 * This function is called from the Micrium RTOS plugin before entering EM1 or 
 * EM2 CPU low power modes. The application should return true if the device may
 * enter the passed low power mode, false otherwise.
 *
 * @param isEm2  This parameter is set to true if the device is attempting to
 * enter the EM2 CPU low power mode, false if the device is attempting to enter 
 * the EM1 CPU low power mode.  Ver.: always
 *
 * @param durationMs The maximum duration in milliseconds that the device will
 * spend in EM1 or EM2 low power mode.  Ver.: always
 */
bool emberAfPluginMicriumRtosOkToEnterLowPowerCallback(bool isEm2,
                                                       uint32_t durationMs)
{
  return true;
}

/** @brief
 *
 * This function is called from the Micrium RTOS plugin before the
 * Application (1) task is created.
 */
void emberAfPluginMicriumRtosAppTask1InitCallback(void)
{
}

/** @brief
 *
 * This function implements the Application (1) task main loop.
 *
 * @param p_arg Ver.: always
 */
void emberAfPluginMicriumRtosAppTask1MainLoopCallback(void *p_arg)
{
  while (true) {
    // Main loop
  }
}

/** @brief
 *
 * This function is called from the Micrium RTOS plugin before the
 * Application (2) task is created.
 */
void emberAfPluginMicriumRtosAppTask2InitCallback(void)
{
}

/** @brief
 *
 * This function implements the Application (2) task main loop.
 *
 * @param p_arg Ver.: always
 */
void emberAfPluginMicriumRtosAppTask2MainLoopCallback(void *p_arg)
{
  while (true) {
    // Main loop
  }
}

/** @brief
 *
 * This function is called from the Micrium RTOS plugin before the
 * Application (3) task is created.
 */
void emberAfPluginMicriumRtosAppTask3InitCallback(void)
{
}

/** @brief
 *
 * This function implements the Application (3) task main loop.
 *
 * @param p_arg Ver.: always
 */
void emberAfPluginMicriumRtosAppTask3MainLoopCallback(void *p_arg)
{
  while (true) {
    // Main loop
  }
}
